#include <iostream>
using namespace std;

int main() {
    string metrics[] = {"Revenue", "Net Income", "EPS"};

    for (int i = 0; i < 3; i++) {
        cout << "Processing: " << metrics[i] << endl;
        cout << "Displaying " << metrics[i] << " Trend Graph..." << endl;
    if (metrics[i] == "Revenue") {
            cout << "Displaying detailed Revenue Trend Graph..." << endl;
        } else if (metrics[i] == "Net Income") {
            cout << "Displaying summarized Net Income Trend Graph..." << endl;
        } else if (metrics[i] == "EPS") {
            cout << "Displaying EPS Graph with annual comparison..." << endl;
        }
    }    }

    return 0;
}
